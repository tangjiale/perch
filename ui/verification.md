# 设计预览验证记录

日期：2026-09-07。范围：静态桌面 UI 设计与有限交互，不是生产应用验收。

## 检查方式

- 使用 agent-browser 驱动 Chromium，打开本地 HTML。
- 主图尺寸 1440 × 1000；看板额外检查 1280 × 800、1024 × 768。
- 两轮截图检查：首轮发现卡片留白与小字号问题，统一收紧间距并改善文本对比度，第二轮确认。
- 全部主图逐张打开检查，应用 Logo 无加载失败；检查尺寸的 document/main 均无横向溢出。
- Windows 图是在浏览器中绘制的标题栏变体，不是 Windows 真机运行证据。

## 交互验证

| 检查 | 结果 |
| --- | --- |
| 把禅道执行 E-238 拖到已完成 | localStatus 变为 done，remoteStatus 仍为进行中 |
| 撤销拖拽 | 本地状态恢复 doing |
| Todo 列表 | 显示 12 条任务 |
| 来源筛选为禅道 | 显示 4 条执行 |
| 创建个人任务 | 同一数据集中新增任务，计数更新 |
| 日历放置事件及撤销 | 计划日期改变后可恢复；此项通过 DOM DragEvent 验证 |
| 日历周、日视图切换 | 对应时间网格出现 |
| 应用收藏 | 收藏值与页面同步更新 |
| 应用输入 javascript 协议 | 被校验拦截，显示错误 |
| 应用输入 https 网址 | 新入口创建成功 |
| 禅道管理开关 | 管理入口标志变化，未发生远端写入 |
| 提交连接表单 | 明确提示未发起连接，令牌输入清空，不持久化 |
| 远端状态颜色独立性 | 本地已完成时，远端进行中仍显示蓝色进行中状态 |
| JavaScript 语法 | app.js / secondary.js 的 node --check 均通过 |

## 样式检测

运行一次 impeccable detect。发现次要文字 hover 对比度不足和小字号，已提高灰色文字对比度并统一关键元信息字号。其 Geist 常见字体提示属于审美建议；本项目以系统中文可读性和用户浅色工作台参考为优先。窗口栏的固定高度与居中排版属于桌面窗口示意，按截图检查。

## 未验证范围

没有禅道地址与账号，因此未验证开源版 22.0 的认证、API、对象权限、读写或同步。未验证 Tauri 原生窗口、macOS 签名、Windows 安装、数据库、持久化、通知、跨日时区边界、完整键盘/屏幕阅读器覆盖以及大量任务性能。设计文档中的正式目标并不等于预览已实现所有功能。

## 独立检查

独立代理逐张检查 10 张截图并只读核对文档和源码，结论为 ship，范围限定为设计文档、界面图和静态预览。没有发现阻塞本轮设计交付的问题。其远端状态颜色建议已修复，并完成上述针对性验证。

## 应用描述与四列布局更新

后续按用户要求增加应用 description 字段、卡片描述、新增/编辑维护和描述搜索，并将桌面应用列表改为四列。

- 描述编辑回显、保存、关键词搜索和新增描述均通过浏览器交互验证。
- 2113 / 1440 / 1280px 宽度为四列，1024px 为三列，800px 为两列，390px 为一列；均无页面或主区横向溢出，12 张卡片描述和 Logo 正常加载。
- 已更新 `05-apps.png`，并逐张检查 1440、1024、390px 截图，无文字重叠。JS 语法检查通过。
- 以上为本次针对性验证，未重新执行完整应用验收；会话内保存、刷新重置的预览边界不变。

## 大模型、Agent 与聊天更新

新增供应商与模型配置、Agent 管理及聊天设计预览。浏览器验证结果：

- 供应商/模型引用删除保护、新增供应商、配置地址保存、新建模型通过。
- 试用密钥未进入 aiStore；测试和保存后输入清空，无真实模型请求。
- 新供应商模型可在 Agent 下拉框立即选择；嵌入模型不会出现。
- Agent 创建、配置保存和开始聊天链路通过，模块独立验证覆盖复制、启停及历史引用删除保护。
- 发送消息、演示回复标记、停止生成、停止后无迟到回复通过。
- 切换 Agent 创建空白独立会话，旧会话保留；供应商停用后旧会话输入禁用。
- 中文输入法组合 Enter 不会发送；已有会话保持创建时 Agent 配置快照。
- 1440 × 1000 主稿及 1024 × 768、390 × 844 辅助检查无页面横向溢出。小屏检查不等于已交付移动 App。
- 模型能力、参数和协议只做配置预览，供应商真实鉴权、流式接口、多模态附件、工具调用、原生安全存储和持久化仍未实现。

## 项目管理与任务关联更新

项目列表、项目详情、任务所属项目已接入同一个项目与任务集合。使用 agent-browser 在 Chromium 中运行 `ui/project-checks.js`，23 项交互断言全部通过：

- 项目总数、来源筛选、同名本地与禅道项目的独立 ID。
- 项目内创建任务默认关联，项目看板按 ID 过滤。
- 个人任务可改到禅道项目，来源仍为个人任务；原项目与目标项目计数同步更新。
- 禅道所属项目只读，注入同名表单字段提交也不改变原始远端项目关系。
- 项目改名后任务展示名更新，关联 ID 保持。
- 有任务项目不能删除；空项目二次确认可删除。
- 独立源码审查发现删除当前筛选项目后保留失效过滤，已修复并加入回归；删除后恢复全部项目筛选。
- 项目看板入口从任务列表切回看板；拖拽与撤销仅改变个人状态，不改远端状态或项目归属。
- 同步弹窗明确未连接、未执行同步；应用与聊天导航正常。

新增 `13-projects.png`、`14-project-detail.png`、`15-task-project.png`，均为 1440 × 1000。项目页另检查 1024 × 768 和 390 × 844，document 与 main 无横向溢出。首轮检查后收紧桌面项目行间距，第二轮截图确认；主页面与抽屉文字无重叠。浏览器没有报告脚本错误，app.js、secondary.js、projects.js、project-store.js 和检查脚本的语法检查通过。

复查方式：先在 agent-browser 打开本地 HTML 并刷新，再执行 `npx --yes agent-browser --session workbench-ai-final eval --stdin < ui/project-checks.js`。脚本会更改预览会话中的示例项目与任务，完成后刷新恢复。

项目来源、远端 ID 与关联目前仍为示例数据，没有发起禅道请求。真实项目拉取、执行内任务映射、远端关系变更、无权限异常处理与事务持久化是设计要求，未完成运行时验证。

## 视觉精修更新

- 共用窗口、侧栏、按钮、字段、字体和表面采用中性灰与石墨色，状态色保持各自含义；项目列表重新调整字号、行高和任务状态分布。
- 23 项项目交互回归全部通过；状态分布由共享任务集实时计算，保留无障碍数量标签和悬停说明。
- 对项目、总览、任务、应用、日历、设置、模型、Agent、聊天共 9 个页面，在 1440、1024 和 390px 宽度检查页面与主区溢出及图片加载。窄屏总览发现溢出，已通过网格最小宽度及文本换行修复；窄屏统计改为两列，避免中文竖排。
- 1440 × 1000 的项目、总览、看板、应用、聊天截图已逐张检查；另检查项目与聊天的 1024 / 390px 截图。桌面应用四列与 Logo 正常。
- 此次为视觉与布局调整，原有静态会话、演示聊天及未连接禅道的边界保持。

## 设置 Tab 更新

设置由纵向锚点菜单改成统一横向 Tab。浏览器 9 项断言通过：四个选项、默认连接面板、范围与权限独立显示、三个禅道面板切换保留输入、方向键移动焦点、大模型选中态与配置显示、从模型返回权限保留开关、Tab 键序列仅包含一个选项。外部“配置禅道”入口直接选中连接 Tab。

1440 × 1000 主稿和 390 × 844 窄屏检查已完成；连接与模型主区没有横向溢出，窄屏 Tab 栏单独横向滚动。secondary.js、models.js、app.js、projects.js 语法检查通过。

## 访问令牌帮助

访问令牌标签增加 circle-alert 图标，点击展开帮助，支持再次点击、外部点击、关闭按钮和 Esc 收起。8 项浏览器断言通过，帮助按钮不会提交连接表单，Esc 关闭后焦点返回触发按钮。1440 × 1000 与 390 × 844 检查中弹层位于屏幕内，窄屏自动向上展开，长内容独立滚动。

获取步骤依据 2026-09-07 读取的禅道官方文档 https://www.zentao.net/book/api/1397.html：REST API v1 的 POST /api.php/v1/tokens，JSON 传入 account/password，并读取返回的 token。没有发起实际认证请求，未验证用户的 22.0 实例配置。

## 应用分类管理

新增分类下拉框右侧的管理链接和紧凑管理弹框。`ui/category-checks.js` 在浏览器中通过 22 项断言：新增、重名/系统名称/空白校验、改名同步应用与当前筛选、特殊字符转义、删除前迁移选择、迁移引用、父表单 DOM 与草稿保留、同一 Logo File 对象保留、分类下拉更新、焦点恢复、Escape/遮罩仅关闭当前管理弹框、清空分类后归入未分类、原草稿继续保存。

1440 × 1000 的入口与管理弹框截图和 390 × 844 弹框截图均已检查。独立代理只读审查提出的父表单节点保留、关闭层级、筛选引用与总览分类硬编码问题均在本次实现中处理。分类同样只在页面会话内保存，刷新重置。

## 技能、知识库与聊天关联

`ui/resource-checks.js` 在浏览器中通过 27 项集成断言：

- 技能与知识库导航、新建技能、Markdown/YAML 元信息导入预览、正文 HTML 转义、确认导入、无效 JSON 拦截。
- Agent 关联技能、引用删除保护、新会话技能快照、技能改动不覆盖历史配置。
- 创建多个知识库、只选已启用 embedding 模型，九类扩展名文件添加、File 对象保留、全部保持 pending。
- 拒绝重复文件、空文件、不支持的格式和大于 30 MB 的文件。
- 聊天选库保留输入草稿、生成期间禁用选库、逐消息知识库快照、切换库不改变历史消息、演示回复明确没有检索。
- 历史会话对知识库的删除保护、知识库对向量模型的删除保护和模型 ID 修改保护、模型停用时知识库不可用、取消选库恢复普通聊天。
- 文档删除确认与删除结果。

技能模块另由独立代理完成 14 项 VM 导入与引用边界检查。知识库模块独立检查了超长字段提交及 390px 文档操作列布局。真实浏览器添加 `ui/examples/project-brief.md` 和 `meeting-notes.txt`，使用下载按钮下载原文件，经 `cmp` 验证与原文件完全一致。

新增 26–31 主图均为 1440 × 1000，逐张打开检查；技能、知识库详情、Agent、模型、聊天额外在 1024 × 768 和 390 × 844 检查，document/main 无横向溢出。文档列表与聊天内容区域保留正常纵向滚动，新的独立浏览器会话没有脚本错误。

验证只覆盖静态预览。九类格式检查使用带扩展名的测试 File 验证接收和校验，没有验证 Office/PDF 内容解析。知识库 File 对象随刷新丢失，尚未上传到后端；没有真实向量模型请求、分块、索引、检索、文档引用生成或技能执行。技能导入目前支持单个 MD/JSON 文件，不包含 ZIP 包与脚本资源安装。

## 品牌版本与任务结束时间

侧栏品牌名称后增加 `v0.1.0`；任务的新建与详情表单增加开始和结束时间，所有示例任务统一包含 `end` 字段。结束时间按同日个人排期处理，与截止日期、完成状态和禅道远端周期分离。

`ui/time-version-checks.js` 在 Chromium 中通过 17 项断言：版本显示、详情回显、错误输入保留、结束时间保存与重开、日期依赖、禅道个人排期隔离、新建任务、缺少开始时间拦截，以及总览、看板、列表、月历、周历和全天任务显示。`end <= time` 会阻止保存并在当前表单显示错误。

新增 `32-version-overview.png`、`33-task-end-time.png` 两张 1440 × 1000 主图，并检查 `34-task-end-time-compact.png`（390 × 844）。品牌、时间段和抽屉字段无重叠；折叠侧栏隐藏品牌文字及版本标签。app.js 与检查脚本的 JavaScript 语法检查通过，浏览器未报告脚本错误。
