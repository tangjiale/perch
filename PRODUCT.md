# 个人工作台

<!-- impeccable:product-schema 1 -->

## Platform

web

目标是 macOS 与 Windows 桌面应用，允许使用 Tauri。当前交付为桌面 WebView 界面设计与浏览器预览，不是已打包的原生应用。

## Users

个人使用者，在日常工作中管理自己的待办、日程、公司项目执行与常用应用。

## Product Purpose

把个人任务、禅道执行和公司应用入口汇集到同一个工作台，让用户知道今天做什么、工作推进到哪一步、在哪里打开需要的工具。

## Capabilities and Constraints

- 首页总览；Todo 列表；可拖拽状态的看板；每日显示任务的日历；应用卡片目录。
- 增加项目管理，可自行创建本地项目或从禅道读取项目；项目显示来源，任务通过稳定项目 ID 关联。禅道条目保持原始项目/执行/任务层级，个人任务可以自行选项目。
- 四种本地状态：待做、正在做、已完成、已关闭。
- 侧栏品牌名称后显示当前应用版本 `v0.1.0`；正式桌面实现由 Tauri 应用配置提供版本值。
- 任务个人排期包含日期、开始时间与同日结束时间；结束时间必须晚于开始时间。它与截止日期、完成状态、禅道远端周期相互独立。
- 禅道执行作为任务来源的一种，保留远端对象类型。
- 用户先确认个人状态独立管理，随后补充可管理禅道，版本为开源版 22.0。设计采用默认读取、可选显式远端管理；本地拖拽不隐式回写。
- 应用包含名称、网址、Logo 和可选描述；新增/编辑支持维护描述，搜索包含描述，卡片最多显示两行描述，悬停可查看完整文本。
- 应用目录在窗口宽度不小于 1200px 时每行四张卡片，900–1199px 为三列，560–899px 为两列，更窄时为一列。
- 应用分类数量较少，在应用表单分类选择框后提供“管理分类”链接，通过紧凑弹框新增、改名、删除；改名同步引用，删除时迁移到其他分类或未分类，分类管理不丢失应用草稿及已选 Logo。
- 本轮先交付 design.md，再交付 UI 界面图。
- 新增大模型供应商与模型管理，配置 API 地址、协议及密钥；左侧新增多个 Agent 的管理和可选择 Agent 的聊天页。
- 当前仍为设计预览阶段：AI 配置与对话保留在当前会话，回复明确标记演示，不发起模型网络请求，不保存真实密钥。
- 新增技能创建与导入，可在 Agent 配置中选择技能；新增多个知识库，上传 PPT、Word、Excel、TXT、MD、PDF 等文件，并关联模型管理中的向量模型。聊天可以选择知识库，静态预览只保存文件与配置，不声称完成解析或向量检索。

## Brand Commitments

中性浅色。整体布局参考用户图二的浅色侧栏、搜索、细边框、紧凑信息层级；日历参考图一的小日历、来源筛选和月历结构。截图是视觉参考，其中的文字与数据不是额外需求。

## Evidence on Hand

用户提供了两张参考截图及禅道开源版 22.0。尚未提供地址、账号、真实任务或公司应用清单。所有界面内容采用明确标注的示例数据，不代表实际接入。

## Stack

设计建议：Tauri 2 + React + TypeScript + SQLite；本轮采用静态 HTML/CSS/JavaScript 制作可截图的设计预览。正式工程技术选型在实现阶段确认。

## Open Decisions

产品正式名称、禅道认证方式和账号权限、执行筛选规则、通知、重复任务与多设备同步待确认。
