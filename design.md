# 栖点 · Perch · 产品与界面设计

## 2026-09-08 应用图标

应用表单使用中文「选择图片」入口、40px 图标预览和文件名/获取状态。输入有效网址后自动读取同源网站图标，获取不到时可手动上传；提供图标按钮重新获取。手动选图优先，后返回的网络结果不得覆盖手动图片。已有应用图标保持不变，只有显式重新获取或上传才替换。沿用当前浅色/深色玻璃控件。

## 2026-09-08 通用设置与深色模式

设置的「关于」前增加「通用设置」，包含外观和会话字号。外观使用三个并排图标单选项：浅色、深色、跟随系统；默认跟随系统，切换即时生效并保留本机偏好。会话字号默认 14px，可设置 12–24px，仅影响消息正文及其中的代码与表格，不改变导航和输入框字号。

深色以中性炭灰背景、浅灰正文、蓝绿交互色为基础，保留玻璃材质的透明层次与细高光；任务状态使用独立蓝色、绿色和琥珀色。外壳、总览、项目、任务、日历四视图、应用、聊天、所有设置页、表单、弹窗、玻璃下拉、通知均按同一主题适配。系统日期控件通过 `color-scheme` 适配，支持减少透明度与高对比偏好。

主题入口为 `src/lib/appearance.ts`，样式为 `src/theme.css`。原型 `ui/` 仍是历史浅色图稿，当前深色交付以正式 React 应用为准。

## 2026-09-08 全局选择器与命名

应用显示名称采用「栖点 · Perch」，表达任务、知识与 AI 助手汇聚的个人工作入口。侧栏中文名称为栖点、英文为 PERCH；保留现有 Logo、应用标识及 `.perch` 数据目录，名称变化不触发数据迁移。

正式 React 应用全部 16 处原生下拉统一为 `GlassSelect`，以 Radix Select 实现键盘、焦点、滚动和碰撞处理。触发器高度 36px，银白半透明材质、顶部高光、蓝绿焦点；浮层背景模糊、圆角、柔和投影，选中项有勾选。聊天 Agent 与知识库选择内置对应图标。

菜单在原生 dialog 内挂载，避免被 top layer 遮挡；长文本在触发器截断、菜单内换行。保留空值、必填和禁用语义，支持减少动态效果及减少透明度偏好。此为 macOS 26 Liquid Glass 启发的 WebView 实现，非 Apple 原生材质 API。

## 2026-09-07 实装视觉更新：浅色液态玻璃

本节为用户对正式 Tauri App 的最新视觉要求，优先于下面的历史图稿说明。

- 保持桌面工作台的信息密度与现有业务功能，采用 iOS 26 Liquid Glass 启发的浅色材质。WebView 用半透明、背景模糊、白色边缘反光和有方向的柔和阴影实现，不宣称使用 Apple 原生 Liquid Glass API。
- 主体保持中性银白，蓝绿仅用于操作强调和品牌。玻璃主要用于侧栏、顶栏、分段控件、弹窗和聊天输入；表格、正文及日历网格保持高可读性。
- 日历严格恢复 `ui/screenshots/04-calendar-month.png` 的结构：左侧小月历、我的日历来源选择、未排期任务；右侧今日/前后导航、日期标题、日/周/月/列表和主日历。保留 FullCalendar 的成熟排期逻辑，替换默认工具栏与字体图标。
- 导航箭头使用 Lucide SVG；选中分段为明亮玻璃底和深色文字。日历默认月视图，周一为每周首日，全天区域使用中文。
- Logo 源文件为 `public/workbench-logo.svg`：层叠面板表示工作空间聚合，勾选表示任务完成，星点表示 Agent。采用银白、蓝绿透明层次；相同图形用于侧栏、favicon 和 macOS/Windows 图标。
- 减少透明度模式提供不透明替代，减少动态效果模式禁用位移动画。日期按钮、工具栏和 Logo 有固定尺寸，避免动态文字挤压与布局跳动。

版本：0.1.0 · 2026-09-07 · 状态：首版设计提案

## 1. 本轮交付与设计定位

### 技能与知识库扩展

- 左侧增加技能、知识库两个独立菜单，延续中性浅色和紧凑工作界面。
- 技能支持名称、描述、指令正文、启停、新建、编辑、导入与删除。导入 Markdown / SKILL.md 时通过 YAML 解析器读取元信息，导入 JSON 时通过 JSON 解析器校验；导入先预览确认，不执行文件里的指令或脚本。
- Agent 配置可以选择多个已启用技能，已有对话保留创建时技能内容快照。技能正文不作为工作台系统指令执行。
- 可以创建多个知识库，每个库关联稳定的供应商 ID 与向量模型 ID，仅允许选择已启用的 embedding 模型。修改被知识库引用的模型能力或远端标识前，必须先解除引用，避免向量空间混用。
- 知识库接收 PPT/PPTX、DOC/DOCX、XLS/XLSX、TXT、MD、PDF，支持多文件上传、文档列表、文件状态、删除与原文件下载。单文件上限 30 MB，拒绝空文件与不支持格式；同名文件不静默覆盖。
- 当前静态预览只在会话内保留 File 对象及元信息，状态为待解析/未索引；没有真实上传、文档提取、分块、向量化或检索，不伪造索引完成与引用。
- 聊天可选择一个知识库或不使用知识库；选择记录在会话中，发送消息时保留知识库 ID、名称及向量模型信息快照。模型不可用时提示重新选择；生成期间锁定选择，避免回复上下文漂移。
- 已被会话引用的知识库不可直接删除，文档删除需要确认。正式实现需处理解析失败、索引重建、向量维度和模型版本一致性、文档权限与可追溯引用。

先形成本文档，再制作总览、任务看板、任务列表、日历、应用目录、禅道连接与任务详情界面图，并提供同一套界面的可交互设计预览。预览使用示例数据；本轮不声称完成 Tauri 安装包、真实禅道接入或桌面通知。

产品暂称「个人工作台 / Workbench」，当前设计预览版本为 `v0.1.0`，在侧栏品牌名称后显示。目标平台为 macOS、Windows。核心使用场景是每天打开工作台，查看当天计划，推进工作，快速进入公司系统。正式接入 Tauri 后，界面版本号应从应用配置读取，避免多处硬编码漂移。

视觉以用户图二为基准：固定左侧导航、轻量顶部工具栏、细边框、信息密度适中。日历沿用图一的日期网格、小日历与任务来源结构，转换为中性浅色。引用截图中的品牌、项目名称、数量和水印不进入产品要求。

### 视觉精修（2026-09-07）

根据“质感不强”的反馈，保留浅色工作台结构，调整共享视觉规范。以下规范优先于初版样式参数：

- 窗口与侧栏采用中性灰 `#f2f2f3`，内容面保持白色，次级面为 `#fafafa`；边框去除蓝灰倾向。
- 主要操作采用石墨色 `#2c2c30`；蓝、绿、琥珀仅用于状态、来源和必要交互强调。导航选中项使用白色表面、深色图标与细微阴影。
- 使用平台界面字体与中文系统字体。页标题 24px，任务及项目主文本 14–15px，描述 12–13px，主要控件 13px；字距保持 0。
- 控件统一 34px 高度、清晰边界和轻微按压反馈。阴影用于按钮、输入工具和浮层，内容分区不使用大阴影。
- 项目列表以 88px 行高组织三个信息层，调整列宽和对齐；关联任务使用真实数量派生的状态分布条，悬停和无障碍标签保留四种状态的具体数量。
- 看板使用浅色列背景和白色任务卡；应用保持桌面四列，并提高描述可读性。聊天使用一致的中性选中态，收紧输入区域。
- 新图稿 16–20 体现当前视觉；早期图稿作为迭代记录保留。
- 设置采用统一横向 Tab：禅道连接、同步范围、管理权限、大模型管理。选中后只显示对应面板，不再使用左侧锚点菜单或纵向堆叠内容；支持左右方向键定位、Enter/空格选择，小窗口可横向滚动 Tab 栏。连接、范围、权限面板切换保留当前输入，真实凭据仍不持久化。新增图稿 21–22。

### 视觉方向比较

| 方向 | 判断 |
| --- | --- |
| 中性浅色效率工作台 | 采用。白色内容区、浅灰导航、克制的蓝色操作强调，符合参考图与长期使用场景。 |
| macOS 毛玻璃与大圆角 | 不采用。跨 Windows 一致性较弱，且弱化密集任务信息。 |
| 大面积彩色项目面板 | 不采用。只保留状态色，不让项目配色占据整个页面。 |

区别性细节：同一任务在总览、列表、看板和日历中保持一致的来源标识、状态与日期；视图切换保留上下文。信息本身成为视觉重点。

## 2. 产品结构

```text
个人工作台
├── 总览：今日任务 / 近期日程 / 任务状态 / 常用应用
├── 项目：全部 / 本地项目 / 禅道项目 / 项目任务
├── 我的任务
│   ├── 看板：待做 / 正在做 / 已完成 / 已关闭
│   └── 列表：同一任务集，可筛选、排序
├── 日历：月 / 周 / 日 / 列表
├── 应用：全部 / 收藏 / 分类
├── Agent：助手列表 / 提示词 / 模型与参数
├── 聊天：会话列表 / 选择 Agent / 消息与输入
└── 设置
    ├── 禅道连接：连接配置 / 同步范围 / 同步记录
    └── 大模型管理：供应商 / API 配置 / 模型目录
```

Todo 不是独立于看板的另一套数据。所有任务视图基于同一任务集合，侧栏不重复设置「Todo」和「任务看板」两个竞争入口。

## 3. 页面规格

### 3.1 统一桌面框架

- 主稿尺寸 1440 × 1000，次级检查 1280 × 800 和 1024 × 768。
- 桌面标题栏 38px，顶部工具栏 64px，左侧导航 216px，内容边距 32px。
- macOS 在左上保留系统窗口控制区域；Windows 使用右上系统窗口按钮。网页预览中的窗口符号仅作视觉示意，不伪装为有效原生窗口 API。
- 顶部：页面路径、全局搜索、创建任务。示例预览持续标记「示例数据」。
- 侧栏：品牌、主要页面、任务来源筛选、设置、个人空间。当前项用浅蓝底与加深文字表达。
- 宽度不足 1200px 时缩减内容边距，辅助日程面板下移；1024px 维持四列看板但减少留白。小于 900px 的浏览器预览采用可折叠导航与局部看板横向滚动。
- 主要验收是 macOS / Windows 桌面尺寸，移动端不属于首版产品范围。

### 3.2 首页总览

上方为页面名与当前日期，随后是无独立外框的状态汇总带。正文左侧是今日任务，右侧是紧凑日历与当天安排。下方是常用应用与近期执行。

- 汇总显示待做、正在做、已完成、已关闭，来源于实际任务集合；逾期单独提示，不算第五种状态。
- 今日任务按明确个人排期筛选，展示标题、来源、状态、时间和优先级。
- 点击状态汇总进入对应筛选的任务页；点击任务打开详情；勾选个人任务代表完成，可撤销。
- 常用应用按收藏/最近访问呈现，点击打开系统浏览器。
- 新用户使用空状态：「今天还没有安排」与「创建任务」，不填造统计数据。

### 3.3 我的任务 · 看板

四列固定顺序：待做、正在做、已完成、已关闭。列为无外框区域，只有浅色列标题和细分隔，不把列与任务卡片做成层层嵌套的卡片。

任务卡片展示标题、来源、优先级、个人安排的开始与结束时间、必要的截止提示。禅道执行额外显示外部编号和所属项目。长标题允许两到三行，完整标题在详情中可读。

- 跨列拖拽立即改变本地状态，移动后显示「已移至正在做 · 撤销」。放置区高亮且列数实时变化。
- 卡片操作菜单与详情状态选择器提供非拖拽替代操作，支持键盘。
- 完成代表工作已达成；关闭代表不再继续。关闭可填写原因；完成与关闭均可重新打开。
- 已完成与已关闭保留记录，支持后续按时间归档；首版不自动删除。
- 过滤项：来源、状态、优先级、日期、标题搜索；看板/列表切换保留过滤条件。

### 3.4 我的任务 · 列表

用于快速录入与扫描。行包含完成框、标题、来源、状态、优先级、计划日期与时间段；默认按计划日期与优先级排序。点击标题进入同一任务详情，筛选和看板一致。使用真实表格语义，保留长标题换行能力。

### 3.5 日历

参考钉钉日历的实用结构，采用周一作为一周起点。

- 顶部提供今天、上月/下月、年月、月/周/日/列表切换。
- 左侧辅助区：小月历、个人任务/禅道执行复选筛选、未排期任务。
- 主体月视图为 7 列 × 5 或 6 行的真实日期网格。今天蓝色实心标记，当前选中日期浅蓝底，非本月日期弱化。
- 日期格展示全天任务及带时间任务，最多三条，其余用「+N 项」打开当天列表。来源颜色旁始终保留文字/图标，不仅靠颜色区分。
- 点击日期查看当天安排；点击条目查看任务；拖入日期改变个人排期；周/日视图支持精确起止时间。
- 跨午夜任务按时区拆分日历段，跨日任务在连续日期显示带连续关系的条目。
- 执行周期和个人排期为不同概念。导入一个跨月执行时，不默认每天生成同一条个人待办；用户单独安排个人工作时间。
- 截止日期单独提示；未排期条目进入侧栏，不凭空落在同步当天。
- 逾期为本地时间早于当前且未完成/未关闭，全天截止在当天结束后才算逾期。

### 3.6 应用目录

每个应用使用独立卡片，展示 Logo、名称、网址、描述、分类、收藏和菜单。卡片圆角 8px，Logo 容器 40px，操作在固定位置不随标题或描述长度跳动。描述最多展示两行，超出截断，悬停可查看完整文本。

- 应用目录在窗口宽度不小于 1200px 时每行四张卡片；900–1199px 为三列，560–899px 为两列，更窄时为一列。
- 支持搜索、分类、收藏、创建、编辑、移除和排序；搜索范围包含描述。
- 新增/编辑字段：名称必填、http/https 网址必填、描述可选、Logo 可选、分类可选。
- Logo 支持上传；获取站点图标为可选操作，失败显示名称首字或统一缺省图标。
- 网址校验拒绝 javascript/file 等非预期协议；默认系统浏览器打开，保留完整目标地址以便识别站点。
- 移除只删除本地入口，不影响外部系统；误删可撤销。
- 示例品牌 Logo 仅用于识别对应外部应用，不表示合作关系。

### 3.7 禅道连接

用户最初确认个人状态独立管理，随后补充可操作管理禅道，并明确使用 **禅道开源版 22.0**。采用 **默认读取 + 可选远端管理**，个人状态与禅道状态保持独立。

连接页提供服务地址、账号与认证凭据输入、测试连接、保存连接、选择项目/执行范围、立即同步与同步记录。尚未配置显示「未连接」，不能用示例数据伪装连接成功。

开启管理模式后，在详情提供「禅道操作」。候选操作包括编辑、启动、暂停、完成、关闭，实际入口必须根据禅道 22.0 API 能力、对象当前状态及账号权限生成。通用技能文档不能证明 22.0 的全部执行状态接口可用，因此未核验的操作不承诺可直接执行，可提供「在禅道中管理」。

远端修改前展示确认页，明确执行编号、当前/目标状态、修改字段与影响范围。成功后回读远端再更新缓存；失败保留原状态，超时先回读核验再决定是否重试。过期数据先刷新。普通看板拖拽仍只改变 localStatus，不产生隐式远端写入；删除执行暂不进入首版。

- 远端对象明确是「禅道执行」，不误写为禅道任务。未来读取执行内任务时使用另一种对象类型。
- 以 connectionId + remoteType + remoteId 作为外部唯一键，重复同步不重复创建。
- localStatus 与 remoteStatus 分离。同步只更新远端标题、远端状态、项目及周期，不覆盖本地状态、个人备注和个人安排。
- 导入时根据远端状态初始化一次本地状态；后续同步不再次映射。远端状态映射需按实际禅道版本验证。
- 任务详情展示本地状态、禅道状态、原始编号、最后同步时间及「在禅道中查看」。
- 断网、认证过期或无权限时保留已有缓存，标记同步异常。远端缺失仅标记不可访问，不推断删除，不直接清除本地任务。
- 首版建议手动同步与应用打开时同步，后台周期同步和通知作为后续决策。
- Token/密码不得进入前端持久化、日志或导出；正式应用经受控 Rust 命令调用网络与安全存储，读取 OS 凭据存储或经过审查的密钥存储方案。

### 3.8 任务详情

右侧抽屉宽 440px，保持原视图可见；包含标题、状态选择、来源、优先级、个人排期日期、开始时间、结束时间、截止日期和备注。开始与结束时间表示同一天内的个人排期，不代表完成时间；结束时间必须晚于开始时间。禅道条目的个人排期同样在本地保存，不修改远端周期。禅道条目附原始执行信息和只读属性。本地备注可编辑，远端标题保持只读。保存成功反馈与失败保留输入均需支持。

## 4. 数据模型与一致性

### 项目管理补充

增加独立项目实体，任务通过 `projectId` 关联，名称只用于展示，不能按同名判断同步关联。

- 本地项目支持创建、名称、描述、状态、负责人和截止日期编辑；项目页显示任务总数与四种任务状态计数，点击进入项目详情或已筛选的看板。
- 禅道项目明确显示「禅道项目」、禅道项目 ID、同步来源和状态。预览数据保持「示例 · 未同步」，同步入口未连接时引导配置。
- 禅道关系遵循「连接 → 项目 → 执行 → 执行内任务」。项目、执行和任务是不同对象，现有 E-* 条目仍明确是执行，不冒充禅道任务。
- 外部项目按 `(connectionId, remoteProjectId)` 去重；先导入项目，再根据远端 ID 绑定执行或任务，不能依赖显示名称。同步在一个事务内更新引用，避免任务指向尚不存在的项目。
- 导入的执行保留 `remoteType=execution`、`remoteExecutionId`、`remoteProjectId`；未来执行内任务另加 `remoteType=task`、`remoteTaskId`，并由执行的真实所属项目解析 `projectId`。
- 禅道来源条目的所属项目在本地只读，不能通过任务编辑随意移动；远端项目关系改变后，下次成功同步更新该引用，保留本地个人状态、备注和排期。
- 本地任务可关联本地或禅道项目，也可不关联。关联禅道项目不会把本地任务自动上传为禅道任务，也不会把其来源改为禅道。
- 项目改名后所有视图通过同一 `projectId` 显示新名称；任务筛选和计数仍正确。同名本地项目与禅道项目保持独立。
- 远端项目无权限或不可访问时保留引用并标记异常，不能直接删除项目或丢弃任务。有关联任务的本地项目不能直接删除。
- 当前仍为静态设计预览，不发起真实禅道拉取；模型绑定与界面联动可以实际操作验证，数据库事务及 API 同步需正式接入后验证。

Project 字段：id, name, description, source(local|zentao), status, owner, due, connectionId?, remoteId?, lastSyncedAt?, syncState?。
Task 增加 projectId；个人排期使用 `date`、`time`、`end`，截止日期使用独立的 `due`；外部引用增加 connectionId、remoteProjectId、remoteExecutionId、remoteTaskId、remoteType。任务名称旁保留项目标签，详情显示所属项目及来源；看板/列表支持项目过滤，项目详情创建任务默认绑定当前项目。

### AI 工作流补充（2026-09-07）

用户新增要求：设置管理多个大模型供应商和模型；左侧新增 Agent 管理；新增可选择 Agent 的聊天页。延续中性浅色设计，参考截图中的配置字段，不采用截图的深色外观。

- 供应商：名称、预设/自定义类型、API 地址、协议、API 密钥、启用状态。支持创建与编辑，本地服务允许 http/https 地址；正式应用密钥进入系统安全存储，不进入前端持久化、日志或导出。
- 模型目录：模型 ID、显示名称、类型（对话/视觉/嵌入）、启用状态；支持添加、编辑、移除。获取可用模型和测试连接须按供应商协议实现，预览只说明未请求，不伪造连接成功。
- Agent：名称、描述、图标、系统提示词、供应商及模型引用、temperature、最大输出 Token、启用状态；支持多个 Agent、编辑、复制、停用和开始聊天。
- 嵌入模型不进入 Agent 对话模型选项。被 Agent 引用的模型/供应商删除时阻止并列明关联，避免悬空引用。模型停用后新消息被阻止，并引导选择可用配置。
- 聊天页：左侧历史会话，中间消息区域，下方输入框；顶部显示 Agent 与模型。切换 Agent 新建独立会话，旧对话保留，历史会话不能静默换成另一个 Agent。
- 支持新会话、会话切换、重命名、清除会话、消息复制、发送与停止。中文输入法组合输入期间 Enter 不提交。
- 预览以醒目的「演示对话」状态区分模拟回复；配置和历史仅保留在当前页面会话，刷新重置。示例数据不是已接入的供应商。
- Agent 读取任务、日历、应用以及写入禅道等工具权限另行设计与接入，不能仅凭提示词声明已拥有工具能力；默认不授权远端写入。

页面布局：模型管理采用设置子导航 + 供应商列表 + 右侧配置面板；Agent 使用紧凑的助手卡片与配置抽屉；聊天采用会话导航、消息正文和底部固定输入区，沿用现有字体、细边框与蓝色主操作。此阶段追加对应设计图和有限交互验证。

新增模型：Provider(id, name, kind, baseUrl, protocol, enabled, models)，Model(id, modelId, name, type, enabled)，Agent(id, name, description, icon, providerId, modelId, systemPrompt, temperature, maxTokens, enabled)，Conversation(id, agentId, title, messages, createdAt)。正式存储另设 credentialReference 与会话配置快照。

| 对象 | 关键字段 |
| --- | --- |
| Task | id, title, source, localStatus, priority, scheduledStart, scheduledEnd, dueAt, allDay, notes, createdAt, updatedAt |
| ExternalReference | connectionId, remoteType=execution, remoteId, remoteStatus, remoteTitle, projectName, remoteStart, remoteEnd, remoteUrl, lastSyncedAt, syncState |
| Application | id, name, url, description, logo, category, favorite, order, lastOpenedAt |
| Connection | id, baseUrl, edition, version, account, credentialReference, selectedScope, managementEnabled, capabilities, syncState, lastSyncedAt |

远端数据与个人覆盖字段分层保存。所有视图引用同一任务 ID；统计、日历、列表和看板派生自同一数据集。日期时间保存带时区的时间值，展示按用户时区；示例固定为 Asia/Shanghai、2026-09-07，避免截图随机器日期变化。

## 5. 视觉系统

### 5.1 三层 Token

| 基础层 | 值 | 语义层 |
| --- | --- | --- |
| white | #FFFFFF | surface / panel |
| neutral-50 | #F7F8FA | canvas / sidebar |
| neutral-100 | #F0F2F5 | subtle / hover |
| neutral-200 | #E5E7EB | border |
| neutral-500 | #606A79 | secondary text |
| neutral-900 | #20242C | primary text |
| blue-600 | #3267D6 | primary action / focus |
| blue-50 | #EDF3FF | selection |
| green-700 | #277553 | completed text |
| green-50 | #EAF5EE | completed background |
| amber-700 | #956018 | priority / warning |
| amber-50 | #FFF5E5 | warning background |
| red-700 | #B54745 | error / overdue |

组件层：button-height=36px，input-height=38px，nav-height=42px，card-radius=8px，task-padding=16px，app-logo-size=40px，drawer-width=440px。所有 UI 颜色引用语义 Token。

状态语义：待做灰、正在做蓝、完成绿、关闭灰；禅道来源以小型橙色标记识别。浅背景配深色文字，不采用整列饱和彩色。

### 5.2 字体与排版

操作界面优先系统中文可读性：macOS 使用 PingFang SC，Windows 使用 Microsoft YaHei UI；拉丁与数字建议使用 Geist Sans，未加载时回退到系统无衬线。正文 14px/1.6；次级文字 12px/1.5；页面标题 26px/1.35；分区标题 16px；统计数字 28px。字间距统一 0，数字使用等宽数字特性。

Geist 字体来源：https://fonts.google.com/specimen/Geist 。字体选择是效率型桌面界面的可读性决策，不追求装饰性展示字。

### 5.3 空间、边界与动效

采用 4px 基准：4 / 8 / 12 / 16 / 24 / 32。页面分区使用留白、分隔线与网格；卡片只用于重复任务/应用项。常规卡片用细边框，不叠加明显阴影；抽屉使用柔和侧向阴影。图标采用 Lucide，同类控件统一 18px、1.7px 线宽。

交互动画 120–180ms，仅用于 hover、抽屉和拖动反馈；不做页面层层入场。遵守 prefers-reduced-motion。

首版以浅色为验收目标。暗色预留语义 Token 扩展，不将未验证的暗色支持列为已交付。

## 6. 状态与可访问性

- 加载：区域骨架与同步中状态，避免覆盖全页。
- 空：明确当前无任务/无结果/无应用，提供相应动作。
- 错误：保留用户输入，指出失败原因并提供重试；不得用「已连接」掩盖失败。
- 离线：显示缓存和上次同步时间，个人任务仍可编辑。
- 文本对比度目标 WCAG AA 4.5:1；不以颜色作为唯一状态线索。
- 图标按钮具有可读名称和悬停提示；输入具有可见标签；焦点轮廓可见。
- 抽屉支持 Escape 关闭、焦点约束与返回触发元素；拖动有状态菜单/选择器替代。
- 长标题、空 Logo、跨月日期、大量任务与无搜索结果不破坏布局。

## 7. 后续实现建议

采用 Tauri 2 + React + TypeScript + SQLite。任务状态逻辑与 UI 分离；看板使用成熟拖拽库（如 dnd-kit），正式日历优先评估 FullCalendar 的月/周/日能力。Rust 层负责数据库、禅道网络请求、凭据与系统浏览器；前端不直接保存认证信息。

先完成本地任务与应用持久化，再接禅道读取与受控管理，再交付通知与桌面打包。macOS / Windows 分别验证窗口、键盘快捷键、缩放、时区、外链与更新机制。Windows 安装包和 macOS 签名公证不以浏览器预览验证替代。

## 2026-09-08 设置菜单调整

正式 React 应用的左侧业务导航保留总览、项目、我的任务、日历、应用与聊天。Agent、知识库、技能管理移至设置页，复用原有管理功能。

设置 Tab 顺序：禅道连接、大模型管理、Agent、知识库、技能、MCP、数据与备份、关于。较窄窗口横向滚动 Tab，选中项自动进入可见区域；支持键盘切换和 URL 定位。

MCP 以连接列表和新增/编辑弹框呈现，包含名称、传输方式、配置状态、认证状态及编辑/删除操作。Streamable HTTP 配置服务地址和请求头；STDIO 配置命令、参数和环境变量。认证信息不回显，删除前确认。沿用正式应用的浅色玻璃材质与紧凑控件。

此阶段交付配置管理；不将“配置启用”显示为“连接成功”，不在保存配置时执行命令。服务检测、工具发现与聊天执行为后续集成范围。原 `ui/` 保留为历史设计预览，当前交付行为以 React 应用和 IMPLEMENTATION.md 为准。

## 8. 设计验收清单

- [x] 总览、任务看板/列表、日历、应用、禅道连接、任务详情结构明确。
- [x] 四种本地状态与禅道读取/可选管理边界明确。
- [x] 浅色 Token、布局、字体、状态与数据模型已定义。
- [x] 累计 33 张 UI 主图，32–33 为版本号总览与任务结束时间；另有缩窗检查图。
- [x] 主图 1440 × 1000 完成；看板额外在 1280 × 800 / 1024 × 768 检查，无横向溢出或文字重叠。内容纵向滚动保留。
- [x] 预览中导航、筛选、拖拽状态、任务详情与应用入口完成有限交互验证，详见 ui/verification.md。

### 实际交付状态

初版独立设计检查结论为 ship，仅适用于其已检查的设计稿与静态预览；新增 AI 页面已完成模块验证和浏览器联动检查。项目功能通过 23 项浏览器交互检查，技能、知识库及聊天关联通过 27 项集成检查，版本与时间段通过 17 项检查。33 张主图索引见 README.md；UI 采用用户指定的浅色方向，通过 HTML/CSS 精确渲染中文并截图。未使用图像生成器模拟不可交互界面。

预览仅在会话内保存修改，刷新重置。禅道状态文字与颜色分别由远端状态派生，不随个人状态变化。正式设计目标中的排序、跨日时区处理、数据库、安全存储、同步日志、真实远端操作与原生系统能力尚未实现，不纳入本轮完成声明。

正式实现待确认：产品命名、禅道 22.0 认证/账号权限/筛选规则、通知、重复任务、多设备同步。以上不阻塞本轮设计。
